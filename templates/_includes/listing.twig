
        <section class="text-gray-700 body-font">
            <div class="container px-8 mx-auto py-36 lg:px-4">
              
                <div class="flex flex-wrap text-left">
                {% for post in posts %}
                    <div class="px-8 py-6 lg:w-1/3 md:w-full">
                      <a href="{{ post.url }}" >

                     {% if post.featureImage|length %}
      {% set image = post.featureImage.one() %}
                        <img class="object-cover object-center w-full h-40 mb-6 rounded"
                            src="{{ image.getUrl }}" alt="content">
                            {% endif %}
                            </a>
                        <div class="flex items-center justify-between ">
                         <a href="{{ post.url }}" >
                            <h2 class="mb-3 text-lg font-semibold text-gray-700 lg:text-2xl title-font">{{ post.title }}
                            </h2>
                            </a>
</span>
                        </div>
                        <p class="mb-4 text-base leading-relaxed">{{ post.summary }}</p>
                    </div>
                      {% endfor %} 
             
                </div>
                                  

            </div>
            
        </section>
             
    